# Vite reads env vars prefixed with VITE_ at build time.
# When deploying to Vercel, set these in the project's environment settings.

# API base URL. Either absolute (https://api.example.com/api) or relative (/api).
VITE_API_URL=http://localhost:8000/api
